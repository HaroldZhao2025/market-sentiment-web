# Core
pandas>=2.1
numpy>=1.26
requests>=2.31
tqdm>=4.66

# HTML parsing (EDGAR & general)
beautifulsoup4>=4.12
lxml>=5.2
html5lib>=1.1

# Market data
yfinance>=0.2.50

# NLP / HF
transformers==4.45.2
tokenizers==0.19.1
huggingface-hub>=0.25.0

# Torch CPU for inference on GitHub runner
# We'll install the wheel from the CPU index in the workflow step below,
# but keeping the pin here helps local dev as well.
torch==2.3.1
